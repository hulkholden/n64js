
export const kSpeedHackEnabled = true;

export const kAccurateCountUpdating = false;

// Experimental rectangle edge handling. Both switches can be changed at runtime
// through the Graphics options or n64js.textureRectDebug.options.
export const textureRectOptions = {
  clamp: false,
  instrument: false,
};
